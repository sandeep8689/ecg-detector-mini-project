# Local development — leave empty (Vite proxy handles /api → localhost:8000)
VITE_API_URL=

# Production (Render + Vercel) — set to your Render backend URL
# VITE_API_URL=https://your-ecg-backend.onrender.com
